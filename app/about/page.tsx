import Link from "next/link";
import { InnerHero } from "@/components/loopcodez/InnerHero";
import { LoopMark } from "@/components/loopcodez/LoopMark";
import { founders,originTimeline } from "@/lib/content/site";
import { createPageMetadata } from "@/lib/seo/metadata";
export const metadata=createPageMetadata({title:"About",description:"Umer and Saqlain. Two perspectives connected through design, engineering, and a continuous loop of collaboration.",path:"/about"});
export default function AboutPage(){return <div className="inner-page about-page"><InnerHero eyebrow="The people behind the loop" title="Different minds." accent="Shared momentum." description="We’re Umer and Saqlain. An independent studio connecting thoughtful design with engineering that makes it work."/><section className="studio-belief"><span className="loop-eyebrow">Our point of view</span><h2>A launch is a beginning.<br/><em>Keep the loop going.</em></h2><div className="studio-values">{[{title:"Automation",body:"Let the systems handle the repetition. Give people more room to think, create, and grow."},{title:"Consistency",body:"Build a clear experience, then keep showing up with the same care in every detail."},{title:"Support",body:"Stay connected beyond launch. Listen, improve, and keep the work moving forward."}].map(value=><article key={value.title}><LoopMark/><h3>{value.title}</h3><p>{value.body}</p></article>)}</div></section><section className="founders-section"><div className="inner-section-heading"><span className="loop-eyebrow">A small, connected team</span><h2>Meet the <em>minds.</em></h2></div><div className="founders-grid">{founders.map((founder,index)=><article className="founder-card" key={founder.name}><div className={`founder-monogram monogram-${index}`} aria-hidden="true"><span>{founder.name.charAt(0)}</span><LoopMark/></div><div className="founder-copy"><h3>{founder.name}</h3><span className="loop-eyebrow">{founder.roles}</span><p>{founder.tagline}</p><p>{founder.personal}</p><Link href={founder.github} className="loop-text-link" target="_blank" rel="noopener noreferrer">Explore GitHub <span>↗</span></Link></div></article>)}</div></section><section className="studio-origin"><div><span className="loop-eyebrow">How we got here</span><h2>A shared idea.<br/><em>An open future.</em></h2></div><ol className="origin-timeline">{originTimeline.map(item=><li className="origin-timeline-item" key={item.label}><h3>{item.label}</h3><p>{item.detail}</p></li>)}</ol></section></div>}
